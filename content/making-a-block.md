To have your block display properly, you must:
- Register model
- Register texture
- Register item mode (item `ItemConvertible`)
- Register blockstate file

If it has a weird shape and you want it to cull properly, set it with `getOutlineShape`
